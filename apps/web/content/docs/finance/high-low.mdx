---
title: High–low range
description: Daily high–low band with close overlay.
---

## Range band

Floating stacked areas (`low` + `high − low`) approximate a high–low range. Close is a `Line` on top. No native range series in Recharts.

<ChartHighLow />
