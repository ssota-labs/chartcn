---
title: Candlestick
description: OHLC candles via custom shapes on ComposedChart.
---

## Custom OHLC marks

Recharts does **not** ship candlestick / OHLC series. This demo renders wicks + bodies with `useXAxisScale` / `useYAxisScale` inside a `ComposedChart`. An invisible `Line` on `close` keeps tooltip binding.

<ChartCandlestick />
