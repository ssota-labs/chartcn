---
title: Histogram
description: Frequency distribution via equal-width bins and a BarChart.
---

## Basic

Pre-bin samples, then render counts with `BarChart`. Recharts has no histogram helper — binning is done in app code.

<ChartHistogram />
