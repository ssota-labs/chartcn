---
title: Waterfall
description: Bridge chart via floating stacked bars.
---

## Floating stack

Waterfall is approximated with a transparent `base` stack segment plus a visible `value` segment (`Cell` colors for increase / decrease / total). No dedicated waterfall mark in Recharts.

<ChartWaterfall />
